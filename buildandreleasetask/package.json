{
  "name": "buildandreleasetask",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "tsc && mkdir -p scripts && cp ../scripts/deploy.ps1 scripts/ && cp ../scripts/IisDeploy.psm1 scripts/",
    "test": "tsc && mocha tests/_suite.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "azure-pipelines-task-lib": "^5.2.8"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "mocha": "^10.0.0",
    "@types/node": "^25.5.0",
    "@types/q": "^1.5.8",
    "sync-request": "^6.1.0",
    "typescript": "^4.6.3"
  }
}
